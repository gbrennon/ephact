[package]
name = "ephact-domain"
version = "0.3.0"
edition = "2024"

[dependencies]
thiserror = "2.0.18"
uuid = { version = "1.23.4", features = ["v4"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
tempfile = "3"

[features]
container-integration = []