name: Run tests
description: Run test suite and check coverage (unit tests only; skips container runtime integration tests)

runs:
  using: composite

  steps:
    - uses: ./.github/actions/install-cargo-llvm-cov
    - shell: bash
      run: just test
