name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

jobs:
  validate:
    name: Validate
    runs-on: codeberg-tiny
    steps:
      - uses: actions/checkout@v6
      - uses: ./.forgejo/actions/install-dependencies
      - uses: ./.forgejo/actions/cache-rust-deps
        with:
          cache-key-prefix: ephact
          rustc-version: stable
          include-sysroot: "false"
      - name: Check formatting
        run: cargo fmt --all -- --check

  test:
    name: Test
    runs-on: codeberg-medium
    needs: [validate]
    steps:
      - uses: actions/checkout@v6
      - uses: ./.forgejo/actions/install-dependencies
      - uses: ./.forgejo/actions/cache-rust-deps
        with:
          cache-key-prefix: ephact
          rustc-version: stable
          include-sysroot: "false"
      - uses: ./.forgejo/actions/run-tests

  lint:
    name: Lint
    runs-on: codeberg-medium
    needs: [validate]
    steps:
      - uses: actions/checkout@v6
      - uses: ./.forgejo/actions/install-dependencies
      - uses: ./.forgejo/actions/cache-rust-deps
        with:
          cache-key-prefix: ephact
          rustc-version: stable
          include-sysroot: "false"
      - name: Clippy
        run: cargo clippy --all-targets --locked -- -D warnings

  verify-secrets:
    name: Verify secrets
    runs-on: codeberg-tiny
    if: >-
      github.event_name != 'pull_request' ||
      github.event.pull_request.head.repo.full_name == github.repository
    steps:
      - uses: actions/checkout@v6
      - uses: ./.forgejo/actions/install-dependencies
      - name: Verify GitHub release token
        uses: ./.forgejo/actions/verify-token
        with:
          token: ${{ secrets.GH_RELEASE_TOKEN }}
          secret-name: GH_RELEASE_TOKEN
          validate-url: https://api.github.com/user
          auth-scheme: bearer
      - name: Verify crates.io production token
        uses: ./.forgejo/actions/verify-token
        with:
          token: ${{ secrets.CARGO_REGISTRY_TOKEN }}
          secret-name: CARGO_REGISTRY_TOKEN
          validate-url: https://crates.io/api/v1/me
          auth-scheme: bearer
      - name: Verify crates.io staging token
        uses: ./.forgejo/actions/verify-token
        with:
          token: ${{ secrets.CARGO_REGISTRIES_STAGING_TOKEN }}
          secret-name: CARGO_REGISTRIES_STAGING_TOKEN
          validate-url: https://staging.crates.io/api/v1/me
          auth-scheme: bearer
      - name: Verify Codeberg token
        uses: ./.forgejo/actions/verify-token
        with:
          token: ${{ github.token }}
          secret-name: FORGEJO_TOKEN
          validate-url: ${{ github.api_url }}/repos/${{ github.repository }}
          auth-scheme: token
