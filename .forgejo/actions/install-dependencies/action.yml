name: Install dependencies
description: Install project dependencies

runs:
  using: composite

  steps:
    - name: Install system packages
      shell: bash
      run: |
        if command -v apt-get >/dev/null 2>&1; then
          apt-get update && apt-get install -y bash git curl
        elif command -v apk >/dev/null 2>&1; then
          apk add --no-cache bash git curl
        elif command -v dnf >/dev/null 2>&1; then
          dnf install -y bash git curl
        elif command -v yum >/dev/null 2>&1; then
          yum install -y bash git curl
        else
          echo "No known package manager found. Trying to continue anyway."
        fi

    - name: Install just
      shell: bash
      run: |
        if command -v just >/dev/null 2>&1; then
          echo "just already installed: $(just --version)"
          exit 0
        fi

        arch=$(uname -m)
        case "$arch" in
          x86_64) target="x86_64-unknown-linux-musl" ;;
          aarch64) target="aarch64-unknown-linux-musl" ;;
          *)
            echo "unsupported architecture: $arch" >&2
            exit 1
            ;;
        esac

        curl -fsSL --proto '=https' --tlsv1.2 \
          -o /tmp/just.tar.gz \
          "https://github.com/casey/just/releases/download/1.58.0/just-1.58.0-${target}.tar.gz"
        tar -xzf /tmp/just.tar.gz -C /tmp just
        install -m 0755 /tmp/just /usr/local/bin/just
        rm -f /tmp/just.tar.gz /tmp/just
        just --version

    - name: Install Rust toolchain
      shell: bash
      run: |
        export PATH="$HOME/.cargo/bin:$PATH"
        if ! command -v rustup >/dev/null 2>&1; then
          curl -fsSL --proto '=https' --tlsv1.2 -o /tmp/rustup-init.sh https://sh.rustup.rs
          sh /tmp/rustup-init.sh -y --default-toolchain 1.95.0 --profile default
          rm -f /tmp/rustup-init.sh
        fi
        echo "$HOME/.cargo/bin" >> "$GITHUB_PATH"
        rustup component add rustfmt clippy
        rustc --version
        cargo --version

